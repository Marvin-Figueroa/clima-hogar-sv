---
import { Image } from 'astro:assets';

import carrierLogo from '../assets/brands/carrier-logo.svg';
import panasonicLogo from '../assets/brands/panasonic-logo.svg';
import daikinLogo from '../assets/brands/daikin-logo.svg';
import lgLogo from '../assets/brands/lg-logo.svg';
import mideaLogo from '../assets/brands/midea-logo.svg';
import confortstarLogo from '../assets/brands/confortstar-logo.svg';
import hisenseLogo from '../assets/brands/hisense-logo.svg';
import adinaLogo from '../assets/brands/adina-logo.svg';
import yorkLogo from '../assets/brands/york-logo.svg';
import samsungLogo from '../assets/brands/samsung-logo.svg';
import lennoxLogo from '../assets/brands/lennox-logo.svg';
import innovairLogo from '../assets/brands/innovair-logo.svg';

const brands = [
  { name: 'Carrier', logo: carrierLogo },
  { name: 'Panasonic', logo: panasonicLogo },
  { name: 'Daikin', logo: daikinLogo },
  { name: 'LG', logo: lgLogo },
  { name: 'Midea', logo: mideaLogo },
  { name: 'ConfortStar', logo: confortstarLogo },
  { name: 'Hisense', logo: hisenseLogo },
  { name: 'Adina', logo: adinaLogo },
  { name: 'York', logo: yorkLogo },
  { name: 'Samsung', logo: samsungLogo },
  { name: 'Lennox', logo: lennoxLogo },
  { name: 'Innovair', logo: innovairLogo },
];
---

<section
  id="marcas"
  aria-labelledby="marcas-heading"
  class="overflow-hidden bg-navy py-24 lg:py-32"
>
  <div class="mx-auto max-w-7xl px-6 lg:px-8">
    <!-- Header — h2 mantiene la jerarquía de encabezados; estilado visualmente igual que antes -->
    <h2
      id="marcas-heading"
      class="mb-10 text-center font-body text-sm font-semibold uppercase tracking-widest text-cyan"
    >
      Marcas con las que trabajamos
    </h2>

    <!-- Brand grid — unificado para móviles y escritorio -->
    <ul role="list" class="grid grid-cols-2 gap-8 sm:grid-cols-4 lg:grid-cols-4 pt-4 pb-8">
      {
        brands.map((brand) => (
          <li class="group flex items-center justify-center rounded-xl border border-bone-dark bg-bone px-4 py-5 transition-all duration-300 [@media(hover:hover)]:hover:scale-105 [@media(hover:hover)]:hover:-translate-y-1 [@media(hover:hover)]:hover:border-blue/20 [@media(hover:hover)]:hover:bg-white [@media(hover:hover)]:hover:shadow-xl [@media(hover:hover)]:hover:shadow-blue/10 active:scale-105 active:-translate-y-1 active:border-blue/20 active:bg-white active:shadow-xl active:shadow-blue/10 cursor-pointer select-none touch-manipulation">
            <Image
              src={brand.logo}
              alt={brand.name}
              height={80}
              class="pointer-events-none h-[80px] w-auto object-contain transition-all duration-300 opacity-50 grayscale [@media(hover:hover)]:group-hover:opacity-100 [@media(hover:hover)]:group-hover:grayscale-0 group-active:opacity-100 group-active:grayscale-0"
              draggable="false"
            />
          </li>
        ))
      }
    </ul>

    <!-- Note -->
    <p class="mt-8 text-center font-body text-base text-white/60">
      Instalamos y damos mantenimiento a equipos de todas las marcas principales del mercado
    </p>

    <!-- Trademark disclaimer -->
    <p class="mt-3 text-center font-body text-xs text-white/40">
      Los nombres y logotipos de las marcas mostradas son propiedad de sus respectivos dueños. Clima
      Hogar no está afiliado, patrocinado ni respaldado por ninguna de estas marcas.
    </p>
  </div>
</section>
